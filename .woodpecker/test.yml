when:
  branch: master
  event: push

steps:
  - name: test
    image: alpine:latest
    environment:
      MXTP_ROOT_DIR: ${CI_WORKSPACE}
    commands:
      - apk add --no-cache bash git
      - export MXTP_ROOT_DIR="$(pwd)" && bash lib/bashunit tests/
