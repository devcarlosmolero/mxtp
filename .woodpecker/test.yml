when:
  branch: master
  event: push

steps:
  - name: test
    image: alpine:latest
    commands:
      - apk add --no-cache bash git
      - export MXTP_ROOT_DIR=$(pwd)
      - echo "$MXTP_ROOT_DIR"
      - bash lib/bashunit tests/
